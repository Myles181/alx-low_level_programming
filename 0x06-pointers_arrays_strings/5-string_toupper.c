#include "main.h"
/**
 * string_toupper - converts to uppercase
 * @n: Print out n
 * Return: Always 0 for succes
 */
char *string_toupper(char *n)
{
	int ascii;
	char upr;

	ascii = n + 32;
	upr = ascii;
	_putchar(upr);
}
